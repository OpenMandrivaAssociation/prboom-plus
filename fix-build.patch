--- a/prboom2/CMakeLists.txt
+++ b/prboom2/CMakeLists.txt
@@ -76,6 +76,9 @@ check_include_file("dirent.h" HAVE_DIRENT_H)

 option(BUILD_GL "Enable OpenGL rendering code" ON)
 if(BUILD_GL)
+    if(POLICY CMP0072)
+        cmake_policy(SET CMP0072 OLD)
+    endif()
     find_package(OpenGL)
 endif()
